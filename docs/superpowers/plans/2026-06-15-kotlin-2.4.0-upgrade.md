# Kotlin 2.4.0 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade Kotlin from 2.3.21 to 2.4.0 and align all dependencies to their latest compatible versions

**Architecture:** Update version catalog (libs.versions.toml) and convention plugins to use latest stable versions compatible with Kotlin 2.4.0. Verify build succeeds across all targets.

**Tech Stack:** Kotlin Multiplatform, Gradle Kotlin DSL, Version Catalogs, Convention Plugins

---

### Task 1: Update gradle/libs.versions.toml

**Files:**
- Modify: `gradle/libs.versions.toml`

- [ ] **Step 1: Update all version entries**

```toml
[versions]
# Core Tooling
kotlin = "2.4.0"
agp = "9.0.0"
compose-multiplatform = "1.11.1"
dokka = "2.3.0"
maven-publish = "0.36.0"

# Libraries
coroutines = "1.11.0"
koin = "4.2.1"
ktor = "3.5.0"
kotlinx-serialization = "1.8.0"
androidx-activity-compose = "1.10.1"
```

- [ ] **Step 2: Verify file syntax**

Run: `cat gradle/libs.versions.toml`
Expected: All versions updated correctly

- [ ] **Step 3: Commit**

```bash
git add gradle/libs.versions.toml
git commit -m "chore: upgrade Kotlin to 2.4.0 and align dependencies"
```

---

### Task 2: Update Dokka plugin version in convention plugin

**Files:**
- Modify: `buildSrc/src/main/kotlin/ygdrasil/conventions/kmp-dokka.gradle.kts:10`

- [ ] **Step 1: Update hardcoded Dokka version**

```kotlin
dependencies {
    dokkaPlugin("org.jetbrains.dokka:gfm-plugin:2.3.0")
}
```

- [ ] **Step 2: Verify file**

Run: `cat buildSrc/src/main/kotlin/ygdrasil/conventions/kmp-dokka.gradle.kts`
Expected: Version 2.3.0 on line 10

- [ ] **Step 3: Commit**

```bash
git add buildSrc/src/main/kotlin/ygdrasil/conventions/kmp-dokka.gradle.kts
git commit -m "chore: update Dokka plugin to 2.3.0 for Kotlin 2.4.0 compatibility"
```

---

### Task 3: Verify build configuration

**Files:**
- Modify: None (verification only)

- [ ] **Step 1: Run Gradle sync / configuration**

Run: `./gradlew :shared:dependencies --configuration compileClasspath`
Expected: Configuration succeeds without version conflicts

- [ ] **Step 2: Check for deprecation warnings**

Run: `./gradlew :shared:compileKotlinJvm --warning-mode all 2>&1 | head -50`
Expected: No critical errors (warnings acceptable)

- [ ] **Step 3: Commit** (if any minor adjustments needed)

```bash
git add -A
git commit -m "chore: fix build configuration after Kotlin upgrade"
```

---

### Task 4: Build and test all targets

**Files:**
- Modify: None (verification only)

- [ ] **Step 1: Build shared module for all targets**

Run: `./gradlew :shared:build`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: Run tests**

Run: `./gradlew :shared:allTests`
Expected: All tests pass

- [ ] **Step 3: Verify Dokka generation**

Run: `./gradlew :shared:dokkaHtml`
Expected: Documentation generated successfully

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: verify Kotlin 2.4.0 upgrade builds successfully"
```

---

## Self-Review Checklist

- [ ] All version updates in libs.versions.toml match target versions from design
- [ ] Dokka plugin updated in convention plugin
- [ ] No hardcoded versions remain that conflict
- [ ] Build tasks cover all targets (JVM, Android, iOS)
- [ ] Test execution included

## Notes

- Kotlin 2.4.0 requires AGP 8.5.2+ (current 9.0.0 ✓)
- Compose Multiplatform 1.11.1 is latest stable (June 2026)
- Ktor 3.5.0 uses Kotlin 2.3.21 but is compatible with 2.4.0
- Koin 4.2.1 uses Kotlin 2.1.21 but is compatible with 2.4.0
- kotlinx.coroutines 1.11.0 is latest stable